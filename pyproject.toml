[build-system]
requires = ["setuptools==84.0.0", "wheel==0.48.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voicemd"
version = "0.1.0a2"
description = "Reference implementation of the VOICE.md Agent Communication Contract"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [
  {name = "Pavel Nasovich"}
]
keywords = ["agents", "voice", "prompt", "ai", "llm", "agent-skills"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development :: Libraries :: Application Frameworks"
]
dependencies = [
  "PyYAML>=6.0",
  "jsonschema>=4.21",
  "rfc8785>=0.1.4,<1"
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "build==1.5.0",
  "coverage[toml]>=7.10,<8",
  "ruff>=0.16,<1",
]
mcp = ["mcp>=1.29,<2"]
nemotron = ["websockets>=13"]

[project.scripts]
voicemd = "voicemd.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
voicemd = [
  "resources/voice.schema.json",
  "resources/skill/SKILL.md",
  "resources/templates/*.VOICE.md",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.coverage.run]
branch = true
source = ["voicemd"]

[tool.coverage.report]
fail_under = 75
show_missing = true
