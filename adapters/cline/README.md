# Cline adapter

```bash
voicemd install --target cline --mode auto
```

The installer creates a Cline skill and concise rule. This redundancy is intentional because deployment versions and organization settings may differ.
