# Windsurf adapter

```bash
voicemd install --target windsurf --mode auto
```

Windsurf can discover the universal `.agents/skills` path. The generated rule makes the activation boundary visible in the repository.
