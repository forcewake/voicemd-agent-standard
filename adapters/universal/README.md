# Universal Agent Skill

Copy the skill to:

```text
.agents/skills/voice-contract/SKILL.md
```

This path is recognized by several current harnesses and is the preferred repository-level distribution path. The skill description activates only for human-facing natural language.

Install:

```bash
voicemd install --target universal --mode explicit
```
