# OpenCode adapter

Contextual mode:

```bash
voicemd install --target opencode --mode auto
```

For an explicit always-loaded file, merge the `instructions` entry from `opencode.json.example` into project configuration.
