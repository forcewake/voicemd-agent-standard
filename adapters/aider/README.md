# Aider adapter

```bash
voicemd install --target aider

aider --config .aider.voice.yml
```

Aider reads the file as conventions. This is always-loaded rather than on-demand. For a large contract, point the config to a compact compiled artifact instead.
