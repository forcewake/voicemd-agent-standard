{
  "name": "voicemd-typescript-example",
  "version": "0.1.0-alpha.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:conformance": "tsc -p tsconfig.conformance.json",
    "check": "tsc --noEmit && tsc -p tsconfig.conformance.json --noEmit",
    "conformance": "npm run build:conformance && node generated/conformance-verifier.js ../../conformance/vectors.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
